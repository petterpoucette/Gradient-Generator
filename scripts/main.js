/**
 * main.js is responsponisble for declaring the variables and running the funtions
 * that should run from the start
 */

let gradient = new Gradient(3)
var colors

setBodyGradient()
setList()
newColor()

